'use strict';

module.exports = (app, connection) => {
    // Controllers


    // Para iniciar sesión
    app.route('/login')

        .get((req, res) => {
            
        })

        .post((req, res) => {

        })
}